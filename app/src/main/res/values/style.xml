<resources xmlns:tools="http://schemas.android.com/tools">
    <style name="AppTheme" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <item name="colorPrimary">@color/primary</item>
        <item name="colorPrimaryVariant">@color/primary_variant</item>
        <item name="colorOnPrimary">@color/on_primary</item>
        <item name="colorSecondary">@color/secondary</item>
        <item name="colorOnSecondary">@color/on_secondary</item>
    </style>



    <style name="NoRippleThemeOverlay" parent="@style/Theme.AppCompat.Light">
        <item name="android:colorControlHighlight">@android:color/transparent</item>
    </style>



    <!-- Our custom button style that ensures no Material overlay -->
    <style name="CircularRedButtonStyle" parent="@style/Widget.AppCompat.Button">
        <!-- Force our background shape -->
        <item name="android:background">@drawable/button_red_circle</item>
        <!-- Remove ripple highlight if you don’t want purple on press -->
        <item name="android:colorControlHighlight">@android:color/transparent</item>
    </style>
</resources>
